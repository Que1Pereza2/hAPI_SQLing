    Notes app API that stores the notes in a database
    
    The API must have the following requests:
        getNotes()
        updateNotes()
        addNote()
        deleteNotes()
    
    The Notes must have the following structure:
        ID int
        title String
        descripcion String